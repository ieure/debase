#+OPTIONS: toc:nil author:nil title:nil

* Debase, the DBus<->EIEIO bridge.
  :PROPERTIES:
  :ID:       2055fdfe-f336-4c8c-b238-978bfe84d09c
  :END:

  #+TITLE: Debase, illustrated
  [[file:sorry.jpg]]

  D-Bus is an [[https://en.wikipedia.org/wiki/Inter-process_communication][IPC system]] which is ubiquitous on Linux, and (in this
  author’s opinion) not very good.  Emacs has bindings for interfacing
  with it (see the former point), which are annoying to use (see the
  latter point).

  These days, numerous common system management tasks are implemented
  as D-Bus services rather than tradidional *nix commands, and many of
  the command-line tools themselves are now front-ends which
  communicate via D-Bus.  Mounting and unmounting disks, monitoring
  battery status, controlling display brightness, connecting to
  wireless networks and more are now handled with D-Bus services.

  It makes no sense to shell out to the tools when one could interact
  with them directly via D-Bus, if only it was less annoying to do so.

  Debase frees you from writing repetitive, annoying boilerplate code
  to drive D-Bus services by throwing another pile of abstraction at
  the problem, in the form of unreadably dense, macro-heavy,
  profoundly cursed Lisp.


** A crash course in D-Bus
   :PROPERTIES:
   :ID:       d00772f4-8bfe-4f63-8d7f-ccc43d2586f2
   :END:

   - Bus.  A bus contains services; D-Bus can manage many different
     busses, but the two standard ones are:
     - System bus.  This generally has hardware-interfacing and system
       management services.
     - Session bus.  This is private to the current session, i.e. a
       logged-in user.

   - Services.  A service exists on a bus, and is a set of information
     and operations offered by a program.  Example: =org.bluez= on the
     system bus is the service which manages Bluetooth.

   - Objects.  An object exists within a service, and typically
     represents a resource it manages.  Objects are identified by
     paths; paths are namespaced under the service.  Example:
     =/org/bluez/hci0/dev_01_23_45_67_89_AB= is the path to an object
     representing a specific Bluetooth device.  Because this is part
     of the service, that path doesn’t represent anything in a
     different service, like =org.freedesktop.fwupd=.

   - Interfaces.  An interface is a view into the capabilities of an
     object.  Objects can (and almost always do) support multiple
     interfaces.  Example: =org.bluez.Device1= is a general interface
     for managing pairing/unpairing/connecting/disconnecting from
     Bluetooth devices; =org.bluez.MediaControl1= is an interface for
     media devices, such as speakers or speakerphones.  Since
     =/org/bluez/hci0/dev_01_23_45_67_89_AB= is a media device, it
     supports both interfaces.

   - Properties.  A property is a value attached to an interface,
     which exposes information about an object.  For example, the
     =Name= property in the =org.bluez.Device1= interface of
     =/org/bluez/hci0/dev_01_23_45_67_89_AB= is "Bluetooth Speaker" —
     the name of the device.  The interface can define them as being
     read-only, read/write, or write-only.

   - Methods.  A method is a remote function call attached to an
     interface.  For example, the =VolumeUp()= method in the
     =org.bluez.MediaControl1= interface of object
     =/org/bluez/hci0/dev_01_23_45_67_89_AB= in the =org.bluez=
     service of the system bus increases the volume of "Bluetooth
     Speaker."  Methods can take arguments and return values.


** Debase’s abstractions
   :PROPERTIES:
   :ID:       a7ea0d8e-9923-4cd8-9d15-0e69158fda85
   :END:

   Debase is focused on D-Bus objects, and makes no attempt to model
   busses or services.

   Debase uses EIEIO to create proxy classes which represent D-Bus
   interfaces.  These are generic, and reused for any object needing
   that interface.  These all inherit from =debase--dbus=.

   Proxy classes are solely concerned with interfaces, thus they have
   no information about a specific object or what service/bus/path it
   might exist under.

   Objects inherit from =debase--dbus-object= and one or more proxy
   classes.

   Properties are represented by slots, with getters/setters; D-Bus
   methods are generic functions.

   Do not attempt to use slot values directly; use the accessors.
   Accessors have the form =debase-prop-NAME=, where =NAME= is a
   sanitized version of the D-Bus property name.

   Accessors are =setf=-able, if the D-Bus property is writeable.

   Specific D-Bus objects are represented by instances of a wrapper
   class which defines the bus, service, and object path, and inherits
   from the EIEIO classes which comprise all its interfaces.


** Usage
   :PROPERTIES:
   :ID:       95dd1bd2-b4ed-4b0d-baef-206ea6d5a3d7
   :END:

   Debase is a base layer for building domain-specific functionality.
   It generates the code that handles most of the D-Bus rigamarole,
   which you can build on top of.


*** Classes
    :PROPERTIES:
    :ID:       864cd662-cb9f-40e5-bbc6-d16127910ad6
    :END:

    Debase creates one class per D-Bus interface, along with support
    functions for managing its properties and calling functions on
    it.  These are called /interface classes/.

    To represent a specific D-Bus object, a new class is defined,
    which inherits the classes corresponding to all the object’s
    interfaces.  This is called a /composite class/.

    Classes are easily defined using a =:SPEC=, which is a list of
    =(BUS SERVICE PATH)=; Debase uses reflection to discover the
    interfaces and generate code:

    #+BEGIN_SRC emacs-lisp :session usage-1 :eval always :exports both :results value verbatim
      (require 'debase)

      (debase-define-class network-manager
        :spec (:system "org.freedesktop.NetworkManager" "/org/freedesktop/NetworkManager")

        ;; The generic methods will all get this prefix.  If unspecified,
        ;; "debase-" is used.
        :prefix "nm-")
    #+END_SRC

    #+RESULTS:
    : network-manager

    Even though the =network-manager= class is reusable & may be
    targeted at any D-Bus object, the bus/service/path values from
    =:SPEC= are used as defaults, which is handy for well-known
    interfaces like UPower, UDisks2, NetworkManager, etc.

    Creating an instance is as easy as calling the constructor:

    #+BEGIN_SRC emacs-lisp :session usage-1 :eval always :exports code
      (setq my/nm (network-manager))
    #+END_SRC

    #+RESULTS:
    : #s(#s(eieio--class network-manager "Debase composite class, representing an object with the following interfaces:
    :
    :  - org.freedesktop.NetworkManager" (#s(eieio--class debase--dbus-object "Base class for D-Bus objects." nil [#s(cl-slot-descriptor bus unbound symbol ((:documentation . "Bus the D-Bus service is on."))) #s(cl-slot-descriptor service unbound string ((:documentation . "D-Bus service."))) #s(cl-slot-descriptor path unbound string ((:documentation . "Path to D-Bus object."))) #s(cl-slot-descriptor interfaces unbound list ((:documentation . "Interfaces this object implements.")))] #s(hash-table size 65 test eq rehash-size 1.5 rehash-threshold 0.8125 data (bus 0 service 1 path 2 interfaces 3)) (network-manager udisks2-manager udisks2--base udisks2-manager-base debase-composite--debase--dbus-object&debase-udisks2-manager68 debase-composite--debase--dbus-object&debase-udisks2-manager67 debase-composite--debase--dbus-object&debase-udisks2-manager66 debase-composite--debase--dbus-object&debase-udisks2-manager65) ((:bus . bus) (:service . service) (:path . path)) [] [] #s(#3 unbound unbound unbound unbound) (:custom-groups nil :abstract t :documentation "Base class for D-Bus objects.")) #s(eieio--class nm-network-manager "Debase class corresponding to D-Bus interface \"org.freedesktop.NetworkManager\"" (#s(eieio--class debase--dbus "Base class for D-Bus interface classes." nil [#s(cl-slot-descriptor interface unbound string ((:documentation . "D-Bus interface this class implements.")))] #s(hash-table size 65 test eq rehash-size 1.5 rehash-threshold 0.8125 data (interface 0)) (nm-network-manager debase-network-manager udisks2-udisks2-manager udisks2-manager-base debase929 debase928 debase927 debase925 debase-org\.freedesktop\.UDisks2924 debase-dbus-peer debase-dbus-introspectable debase-dbus-properties udisks2-manager udisks2--base-udisks2-manager debase-udisks2-manager) nil [] [] #s(#5 unbound) (:custom-groups nil :abstract t :documentation "Base class for D-Bus interface classes."))) [#s(cl-slot-descriptor interface "org.freedesktop.NetworkManager" string ((:documentation . "D-Bus interface this class implements."))) #s(cl-slot-descriptor debase-devices unbound t nil) #s(cl-slot-descriptor debase-all-devices unbound t nil) #s(cl-slot-descriptor debase-checkpoints unbound t nil) #s(cl-slot-descriptor debase-networking-enabled unbound t nil) #s(cl-slot-descriptor debase-wireless-enabled unbound t nil) #s(cl-slot-descriptor debase-wireless-hardware-enabled unbound t nil) #s(cl-slot-descriptor debase-wwan-enabled unbound t nil) #s(cl-slot-descriptor debase-wwan-hardware-enabled unbound t nil) #s(cl-slot-descriptor debase-wimax-enabled unbound t nil) #s(cl-slot-descriptor debase-wimax-hardware-enabled unbound t nil) #s(cl-slot-descriptor debase-active-connections unbound t nil) #s(cl-slot-descriptor debase-primary-connection unbound t nil) #s(cl-slot-descriptor debase-primary-connection-type unbound t nil) #s(cl-slot-descriptor debase-metered unbound t nil) #s(cl-slot-descriptor debase-activating-connection unbound t nil) #s(cl-slot-descriptor debase-startup unbound t nil) #s(cl-slot-descriptor debase-version unbound t nil) #s(cl-slot-descriptor debase-capabilities unbound t nil) #s(cl-slot-descriptor debase-state unbound t nil) #s(cl-slot-descriptor debase-connectivity unbound t nil) #s(cl-slot-descriptor debase-connectivity-check-available unbound t nil) #s(cl-slot-descriptor debase-connectivity-check-enabled unbound t nil) #s(cl-slot-descriptor debase-global-dns-configuration unbound t nil)] #s(hash-table size 65 test eq rehash-size 1.5 rehash-threshold 0.8125 data (interface 0 debase-devices 1 debase-all-devices 2 debase-checkpoints 3 debase-networking-enabled 4 debase-wireless-enabled 5 debase-wireless-hardware-enabled 6 debase-wwan-enabled 7 debase-wwan-hardware-enabled 8 debase-wimax-enabled 9 debase-wimax-hardware-enabled 10 debase-active-connections 11 debase-primary-connection 12 debase-primary-connection-type 13 debase-metered 14 debase-activating-connection 15 debase-startup 16 debase-version 17 debase-capabilities 18 debase-state 19 debase-connectivity 20 debase-connectivity-check-available 21 debase-connectivity-check-enabled 22 debase-global-dns-configuration 23)) (network-manager) nil [] [] #s(#3 "org.freedesktop.NetworkManager" unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound) (:custom-groups nil :abstract t :documentation "Debase class corresponding to D-Bus interface \"org.freedesktop.NetworkManager\""))) [#s(cl-slot-descriptor bus :system symbol ((:documentation . "Bus the D-Bus service is on."))) #s(cl-slot-descriptor service "org.freedesktop.NetworkManager" string ((:documentation . "D-Bus service."))) #s(cl-slot-descriptor path "/org/freedesktop/NetworkManager" string ((:documentation . "Path to D-Bus object."))) #s(cl-slot-descriptor interfaces ("org.freedesktop.NetworkManager") list ((:documentation . "Interfaces this object implements."))) #s(cl-slot-descriptor interface "org.freedesktop.NetworkManager" string ((:documentation . "D-Bus interface this class implements."))) #s(cl-slot-descriptor debase-devices unbound t nil) #s(cl-slot-descriptor debase-all-devices unbound t nil) #s(cl-slot-descriptor debase-checkpoints unbound t nil) #s(cl-slot-descriptor debase-networking-enabled unbound t nil) #s(cl-slot-descriptor debase-wireless-enabled unbound t nil) #s(cl-slot-descriptor debase-wireless-hardware-enabled unbound t nil) #s(cl-slot-descriptor debase-wwan-enabled unbound t nil) #s(cl-slot-descriptor debase-wwan-hardware-enabled unbound t nil) #s(cl-slot-descriptor debase-wimax-enabled unbound t nil) #s(cl-slot-descriptor debase-wimax-hardware-enabled unbound t nil) #s(cl-slot-descriptor debase-active-connections unbound t nil) #s(cl-slot-descriptor debase-primary-connection unbound t nil) #s(cl-slot-descriptor debase-primary-connection-type unbound t nil) #s(cl-slot-descriptor debase-metered unbound t nil) #s(cl-slot-descriptor debase-activating-connection unbound t nil) #s(cl-slot-descriptor debase-startup unbound t nil) #s(cl-slot-descriptor debase-version unbound t nil) #s(cl-slot-descriptor debase-capabilities unbound t nil) #s(cl-slot-descriptor debase-state unbound t nil) #s(cl-slot-descriptor debase-connectivity unbound t nil) #s(cl-slot-descriptor debase-connectivity-check-available unbound t nil) #s(cl-slot-descriptor debase-connectivity-check-enabled unbound t nil) #s(cl-slot-descriptor debase-global-dns-configuration unbound t nil)] #s(hash-table size 65 test eq rehash-size 1.5 rehash-threshold 0.8125 data (bus 0 service 1 path 2 interfaces 3 interface 4 debase-devices 5 debase-all-devices 6 debase-checkpoints 7 debase-networking-enabled 8 debase-wireless-enabled 9 debase-wireless-hardware-enabled 10 debase-wwan-enabled 11 debase-wwan-hardware-enabled 12 debase-wimax-enabled 13 debase-wimax-hardware-enabled 14 debase-active-connections 15 debase-primary-connection 16 debase-primary-connection-type 17 debase-metered 18 debase-activating-connection 19 debase-startup 20 debase-version 21 debase-capabilities 22 debase-state 23 debase-connectivity 24 debase-connectivity-check-available 25 debase-connectivity-check-enabled 26 debase-global-dns-configuration 27)) nil ((:bus . bus) (:service . service) (:path . path)) [] [] #s(#1 :system "org.freedesktop.NetworkManager" "/org/freedesktop/NetworkManager" ("org.freedesktop.NetworkManager") "org.freedesktop.NetworkManager" unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound) (:custom-groups nil :documentation "Debase composite class, representing an object with the following interfaces:
    :
    :  - org.freedesktop.NetworkManager")) :system "org.freedesktop.NetworkManager" "/org/freedesktop/NetworkManager" ("org.freedesktop.NetworkManager") "org.freedesktop.NetworkManager" unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound unbound)


    #+BEGIN_QUOTE
    *Tip*

    The =*Help*= buffer produced with =C-h f network-manager RET=
    lists all available properties and methods.
    #+END_QUOTE

    #+BEGIN_QUOTE


*** Naming
    :PROPERTIES:
    :ID:       a7ac32d2-186f-4850-9034-801c6f294ca5
    :END:

    The name of the composite class is always explicitly provided.

    The names of the interface classes and their methods are always
    generated, based on the D-Bus interface name and =:PREFIX= keyword
    argument.

    Names of properties are also generated, except they have =-prop-=
    inserted between the prefix and D-Bus name.


*** Methods
    :PROPERTIES:
    :ID:       aa5cae9e-eb5f-4849-a5dd-24332253cb1b
    :END:

    D-Bus methods are implemented as Lisp generic methods, specialized
    to the interface class.

    #+BEGIN_SRC emacs-lisp :session usage-1 :eval always :exports both :results value verbatim
      (nm-get-permissions my/nm)
    #+END_SRC

    #+RESULTS:
    : (("org.freedesktop.NetworkManager.enable-disable-network" "yes") ("org.freedesktop.NetworkManager.sleep-wake" "no") ("org.freedesktop.NetworkManager.enable-disable-wifi" "yes") ("org.freedesktop.NetworkManager.enable-disable-wwan" "yes") ("org.freedesktop.NetworkManager.enable-disable-wimax" "yes") ("org.freedesktop.NetworkManager.network-control" "yes") ("org.freedesktop.NetworkManager.wifi.share.protected" "yes") ("org.freedesktop.NetworkManager.wifi.share.open" "yes") ("org.freedesktop.NetworkManager.settings.modify.system" "yes") ("org.freedesktop.NetworkManager.settings.modify.own" "yes") ("org.freedesktop.NetworkManager.settings.modify.hostname" "auth") ("org.freedesktop.NetworkManager.settings.modify.global-dns" "auth") ("org.freedesktop.NetworkManager.reload" "auth") ("org.freedesktop.NetworkManager.checkpoint-rollback" "auth") ("org.freedesktop.NetworkManager.enable-disable-statistics" "yes") ("org.freedesktop.NetworkManager.enable-disable-connectivity-check" "yes"))


*** Properties
    :PROPERTIES:
    :ID:       23d18dde-2372-478b-9418-f8986f5e0ba3
    :END:

    Each D-Bus property has an accessor function with a =nm-prop-=
    prefix.

    #+BEGIN_SRC emacs-lisp :session usage-1 :eval always :exports both :results value verbatim
      (nm-prop-wwan-enabled my/nm)
    #+END_SRC

    #+RESULTS:
    : t

    For properties specified as writable in the D-Bus interface,
    they’re =setf=-able:

    #+BEGIN_SRC emacs-lisp :session usage-1 :eval always :exports both :results value verbatim
      (setf (nm-prop-wwan-enabled my/nm) nil) ; returns the value that was set, nil
      (nm-prop-wwan-enabled my/nm)            ; now returns nil
    #+END_SRC

    #+RESULTS:
    : nil

    *Warning for those who know about EIEIO and/or CLOS*

    While properties are also slots in the object, do not use =oref=
    or =slot-value= to access them.  This will only return the local
    value of the object, which may be unbound.  The accessor function
    is required to actually fetch the current value from D-Bus.


** Building
   :PROPERTIES:
   :ID:       a58e6392-5371-4ff6-adfd-2db6ffb5feae
   :END:

   Many D-Bus interfaces have
